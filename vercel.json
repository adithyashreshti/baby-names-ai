{
  "functions": {
    "api/generate-names.ts": {
      "maxDuration": 60
    },
    "api/numerology.ts": {
      "maxDuration": 60
    }
  },
  "version": 2,
  "builds": [
    { "src": "api/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
